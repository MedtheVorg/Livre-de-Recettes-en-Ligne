const Spinner = () => {
  return (
    <div>
      <span
        className={`inline-block w-8 h-8   rounded-full border-4 border-white border-b-transparent animate-spin`}
      ></span>
    </div>
  );
};

export default Spinner;
